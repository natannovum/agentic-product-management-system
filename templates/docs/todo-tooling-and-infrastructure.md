# Tooling and Infrastructure Todo

> **Template.** Delete this block. Keep this file only if the project has tooling or infrastructure that is itself maintained — a test harness, servers, provisioning scripts, credential handling. If it does not, delete the file; an empty concern is worse than none.

Pending work on everything that supports the product without being the product.

The product itself belongs in `todo-product.md`; published surfaces in `todo-product-marketing.md`; this documentation system in `todo-documentation.md`.

**Why this concern usually gets created late.** It is not the product and it has no user, so nothing forces it into existence — and the work colonises whichever other file is nearest. The symptom is entries filed somewhere they do not belong because there is nowhere better: deployment records in a marketing log, a test-harness backlog under product features. If you recognise that, this is the file that was missing.

---

## <Item title> (<date>)

<What is wrong or missing, and how it is known — a measurement, an error, an observed failure.>

<Why it has not been done, if that is interesting. What it blocks.>

- [ ] <Concrete next step>
- [ ] <Concrete next step>

---
